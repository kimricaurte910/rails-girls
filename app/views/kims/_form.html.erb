<%= form_for(@kim) do |f| %>
  <% if @kim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kim.errors.count, "error") %> prohibited this kim from being saved:</h2>

      <ul>
      <% @kim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
